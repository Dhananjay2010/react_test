import React from "react"

function Submit(){
    return (
        <button type="button" class="btn btn-secondary" onClick={()=>{
            
        }}>Submit</button>
    )
}

export default Submit;